<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>User Login</title>
<%@ include file="components/allcss.jsp"%>
<style>
  <style>
        /* Keep body normal so navbar stays correct */
        body {
            background: linear-gradient(135deg, #0ea5e9, #0369a1);
            margin: 0;
            min-height: 100vh;
        }

        /* Create a login wrapper that centers only login */
        .login-wrapper {
            min-height: 100vh;
            display: grid;
            place-items: center;
        }

        .login-card {
            width: 380px;
            padding: 30px;
            border-radius: 20px;
            background: white;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            animation: fadeIn 0.7s ease;
        }

        .login-card h3 {
            font-weight: 700;
            color: #0369a1;
        }

        .login-card .form-control {
            border-radius: 10px;
            height: 46px;
        }

        .btn-login {
            height: 46px;
            border-radius: 10px;
            background-color: #0369a1;
            color: white;
            font-weight: 600;
        }

        .btn-login:hover {
            background-color: #0ea5e9;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to   { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
	<%@include file="components/navbar.jsp"%>

	<div class="login-card">
		<div class="text-center mb-3">
			<img src="img/user.png" width="80" alt="User Icon">
		</div>

		<h3 class="text-center mb-4">User Login</h3>

		<form>
			<div class="mb-3">
				<label class="form-label">Email</label> <input type="email"
					class="form-control" placeholder="Enter your email" required>
			</div>

			<div class="mb-3">
				<label class="form-label">Password</label> <input type="password"
					class="form-control" placeholder="Enter password" required>
			</div>

			<button type="submit" class="btn btn-login w-100 mt-2">Login</button>
		</form>

		<div class="mt-3 text-center">
			<a href="#" class="text-decoration-none">Forgot Password?</a>
		</div>

		<div class="mt-2 text-center">
			<a href="register.html" class="text-decoration-none">Create New
				Account</a>
		</div>
	</div>

</body>
</html>